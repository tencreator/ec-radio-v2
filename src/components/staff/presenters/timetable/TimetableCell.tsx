import { useEffect, useState } from "react"

interface Props {
    date: string
    time: string
}

export default function Cell({date, time}: Props) {
    return (
        <div>Loading...</div>
    )
}